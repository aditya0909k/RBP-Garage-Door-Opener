import React from 'react'
import { View } from 'react-native'

const gpio = () => {
    return (
        <View>
            onPress={fetch('[IPADDRESS]')}
        </View>
    )
}

export default gpio
